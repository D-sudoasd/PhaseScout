# History notes

PhaseScout started as a local Materials Project CIF GUI.  
Local batch libraries live under `downloads/` and are **not** published to git.

Public releases ship an empty `downloads/` placeholder only.
